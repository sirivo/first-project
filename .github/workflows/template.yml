name: Reusable template

on: workflow_call

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Print statement
      run: echo "Hello from templates ..."
