name: Reusable workflow template call job
on: [push, workflow_dispatch]
jobs:
  workflow-template-call-job:
    runs-on: ubuntu-latest
    steps:
      - name: workflow-template-call
        uses: ./.github/workflows/template.yml
